/**
 * @format
 */

import 'react-native';
import React from 'react';


import FlatList from '../src/pages/Details';

// Note: test renderer must be required after react-native.
import renderer from 'react-test-renderer';

it('Testing...', () => {
  renderer.create(<FlatList />);
});
